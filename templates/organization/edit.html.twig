{% extends 'default/entity.html.twig' %}

{% block title %}{{ 'title.organization.edit %name%'|trans({'%name%': organization.label}) }} · {{ parent() }}{% endblock title %}

{% block headline %}
    <h1>
        <span class="fa fa-building"></span>
        {{ 'title.organization.edit %name%'|trans({'%name%': organization.label}) }}
    </h1>
{% endblock %}

{% block content_settings %}
    {{ form_start(edit_form) }}
    {{ form_row(edit_form.label) }}
    {{ form_row(edit_form.legalName) }}
    {{ form_row(edit_form.acronymDefinition) }}
    <button class="btn btn-primary" type="submit">
        <span class="fa fa-save"></span>
        {{ 'action.organization.edit.text' | trans }}
    </button>
    {{ form_end(edit_form) }}
{% endblock content_settings %}

{% block content_action %}
    {% include 'organization/content-action.html.twig' %}
{% endblock content_action %}