{% extends 'base.html.twig' %}

{% block title %}{{ 'title.search.index'|trans }} · {{ parent() }}{% endblock title %}

{% block headline %}
    <h1>
        <span class="fas fa-fw fa-search"></span>
        {{ 'title.search.result'|trans }}
    </h1>
{% endblock %}

{% block container %}
    <div class="card">
    <div class="card-header">
        {{ form(form_search) }}
        <ul class="nav nav-tabs card-header-tabs" id="entityTab" role="tablist">
            <li class="nav-item">
                <span class="nav-link active" role="tab">
                    <span class="fas fa-address-card"></span>
                    {{ 'search.tab.person %count%' | trans({'%count%': personPaginator.totalItemCount }) }}
                </span>
            </li>
        </ul>
    </div>
    <div class="card-body tab-content">
        <div role="tabpanel" class="tab-pane fade show active" id="persons">
            {% include '_contacts.html.twig' with { 'pagination': personPaginator } %}
        </div>
    </div>
{% endblock container %}